version: "3.9"  # optional since v1.27.0
services:
  web:
    build: .
    ports:
      - "443:443"
    environment:
      - CHANNEL_SECRET=${CHANNEL_SECRET}
      - CHANNEL_TOKEN=${CHANNEL_TOKEN}
      - PORT=${PORT}
